-# %ul.locks.list_as_cols
-#   - @locks.each do |lock|
-#     %li.lock
-#       .name= I18n.t "may_#{lock}".to_sym
-#       -#.description= I18n.t "#{lock}_description".to_sym
-#       %ul.keys{:id => "keys_for_#{lock}"}
-#         = render :partial => '/me/permissions/key', :collection => @keys, :locals => {:lock => lock}


= formy(:table_form) do |f|
  - @locks.each do |lock|
    - f.row do |r|
      - r.label I18n.t "may_#{lock}".to_sym
      - r.input permission_lock_tag(:me, lock, @keys)
      - r.info "user_profile_description_may_#{lock}".to_sym.t


