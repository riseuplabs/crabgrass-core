/ locals: no_title
#wiki-area
  %span.right
    = edit_wiki_link
  %div.wiki
    - if local_assigns[:preview]
      = @wiki.render_preview(preview)
      = wiki_more_link
    - else
      = @wiki.body_html
      = wiki_less_link
