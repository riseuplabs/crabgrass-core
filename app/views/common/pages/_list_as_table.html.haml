- ##
- ## displays a list of pages in a table (ie, using rows and columns). 
- ##
-
-  style = :updated
-  # local_assigns[:columns] ||= [:icon, :title, :updated_by, :updated_at, :contributors_count]
-  colspan = 5
-  heading = true if heading.nil?
-  sortable = false

%table
  - if pages.empty?
    %tr
      %td{:colspan => colspan}
        = I18n.t(:no_search_results)
  - else
    - if heading
      = page_table_header_row(style)
    - pages.each do |page|
      = page_table_row(page, style)
      =# "<tr><td></td><td>#{page.title}</td><td>#{page.updated_by_login}</td><td>#{page.updated_at}</td></tr>"


