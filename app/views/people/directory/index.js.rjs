update_alert_messages(page)  # will clear it if there are none.
page << "$('user_name').focus();"
page.replace_html(:user_list, render('users'))
if @users.count == 1
  page.insert_html(:after, :user_list, big_spinner)
  page.delay 1 do
    page.redirect_to user_path(@users.first)
  end
end
