- cache [current_language, @group.version_cache_key] do
  - users = @group.users.most_recently_active
  = entity_list users, header: :members.t,
    limit: 5,
    more: group_memberships_path(@group)
