- if @navigation[:local].present?
  #local.sidebar.on_left
    #column_left
      %nav#local_nav
        = render(:partial => 'layouts/local/sidebar/sidebar')
    #column_center
      = render :partial => 'layouts/local/content'

- else
  #local.full_width
    #column_center
      = render :partial => 'layouts/local/content'

